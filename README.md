# Helium Diagnostic tool

```
Usage: [options] [get | server] 

Subcommand:
  get
        get info to stdout
  server
        run http server, can omit it
Options:
  -m string
        miner http url (default "http://127.0.0.1:4467")
  -p string
        server listening port (default "8090")
```